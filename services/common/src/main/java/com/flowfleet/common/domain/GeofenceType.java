package com.flowfleet.common.domain;

/** Category of a geofenced area. Used to interpret enter/exit transitions. */
public enum GeofenceType {
    RESTAURANT,
    CUSTOMER,
    WAREHOUSE,
    ZONE
}
