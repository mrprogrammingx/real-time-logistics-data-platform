package com.flowfleet.common.domain;

/** Vehicle class a driver uses. Affects routing speed assumptions in the simulator. */
public enum VehicleType {
    BICYCLE,
    SCOOTER,
    MOTORCYCLE,
    CAR,
    VAN
}
